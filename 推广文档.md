# DeepSeek Team —— 把 Claude Code 变成你的虚拟开发团队

一个纯中文的 Claude Code 多 Agent 配置方案。五分钟部署，你就能在终端里拥有一个**调度官 + 项目负责人 + 码农 + 审核官 + 调试工程师 + 视觉审查官**的虚拟团队。

主会话走 DeepSeek 推理，截图/UI/PDF 走多模态模型识别——各干各的长处，不用一棵树上吊死。

---

## 为什么写这个

用 Claude Code 的人都知道，一个模型单打独斗的问题：聊着聊着就跑偏，上下文窗口里塞满了历史对话，修个 bug 顺手重构了三个文件。

Agent Teams 出来了，但官方的配置方式散落在文档各处，社区方案（oh-my-claude、my-claude-devteam）功能很全，但它们是"全家桶"——要装 npm 包，要配一堆 provider，提示词全是英文，角色名字叫 Sisyphus、Prometheus，中国开发者看着累。

我想要的是一个**五分钟就能用、读完 CLAUDE.md 就知道怎么改、调度官替你操心该用谁**的东西。

v1 只绑 DeepSeek 三个代理。v2 升级为五代理 + 多模态视觉 + CGG 多供应商调度。

---

## 能干什么

你把需求用中文说给调度官，它自动分流：

- **闲聊/知识问答** → 调度官直接回你，不启动团队
- **需求模糊** → 派给项目负责人，出规格书，回来跟你确认
- **规格清晰** → 直接派给码农写代码
- **复杂 bug** → 派给调试工程师定位根因
- **截图/UI/PDF** → 派给视觉审查官，走多模态模型看图分析
- **代码写完** → 自动推给审核官审查
- **审核通过** → 调度官做终审，把结果交给你

全程中文沟通。如果有人写出英文的 Bash 描述，审核官会自动标记——DeepSeek 在英文环境下思维容易切换，中文锚定是故意的设计。

---

## 团队阵容（五个角色）

### 调度官（Boss）
从你收到的第一条消息开始就在。负责：

- 消息分流判断——闲聊直接聊，任务启动团队
- 派发任务给合适的子代理
- 接收所有人的结果
- 做最后的质量终审

**派发完任务立刻回来跟你对话**，列出运行中代理看板，不沉默等待。调度官不写代码——写代码的模型和做判断的模型应该分开，不然很容易"自己写的代码自己查不出错"。

运行在 DeepSeek v4-pro。

### 项目负责人（Project Lead）
团队的"军师"。遇到模糊需求时被调出来：

- 读项目结构，理解现状
- 跟你确认需求边界
- 产出中文规格书——含背景、范围、技术方案、任务拆解、逐任务详述（输入输出/边界/不可做）、验收标准、风险提示

规格书的格式是固定的，码农拿到就能上场。项目负责人不写代码，动了笔就算违规。

运行在 DeepSeek v4-pro。

### 码农（Coder）—— v2 升级：分体式编码

团队的手。v2 核心升级——**分体式码农**：flash 做双手，每次编码前 spawn 一个 pro 子代理做大脑。

流程：
1. 读上下文（CLAUDE.md + 涉及的文件）
2. **激活码农大脑**：把规格书 + 目标代码打包，spawn 一个 pro 子代理输出精确 Edit 指令清单
3. 逐条执行大脑指令，每步加中文注释
4. Edit 匹配失败最多重试 2 次，全败上报调度官
5. `git diff` 自检改动范围
6. 输出中文完成报告（大脑给了什么指令 + 执行情况）

不改任务范围外的代码，不顺手重构，不自己加功能。

运行在 DeepSeek v4-flash，大脑子代理用 v4-pro。

### 审核官（Code Reviewer）
团队的眼睛。只看不改（Write/Edit 权限已禁止），输出结构化审查报告：

- 🔴 致命：硬编码密钥、SQL 注入、空指针……有就必须驳回
- 🟡 重要：异常吞没、边界遗漏、类型滥用
- 🟢 建议：命名、一致性、中文注释缺失

审查结果分三个等级（驳回/警告/通过），只有"通过"才能走到调度官终审。

运行在 DeepSeek v4-flash。

### 调试工程师（Debugger）—— v2 新增

复杂 bug 的"法医"。码农搞不定的 bug 交给他：

- 读 git diff / 日志 / 调用链
- 定位根因（不修表面症状）
- 输出最小修复方案
- 不写新功能——只做最小修复

运行在 DeepSeek v4-pro，需要深度推理能力。

### 视觉审查官（Visual Inspector）—— v2 新增

团队的眼睛（多模态）。DeepSeek 是纯文本模型看不见图，他来填这个缺口：

- 截图 UI 审查：布局、对齐、颜色、文字溢出
- 架构图/流程图理解：提取节点关系，用文字描述流程
- PDF/文档内容提取

**独立走多模态模型**（千问 qwen3-vl-flash / GPT-4o / Claude / Gemini 可选），不和 DeepSeek 抢资源。

---

## 怎么装

### 前提

- **Claude Code CGG**（必须，v2 新增）：`npm install -g claude-code-cgg`
- DeepSeek API（或其他 Anthropic 兼容端点）
- 一个多模态 API Key（视觉审查官需要）

### 安装步骤

```bash
# 1. 克隆仓库
git clone https://github.com/2951617655-dotcom/deepseek-team.git

# 2. 部署子代理（5 个）
cp deepseek-team/agents/*.md ~/.claude/agents/

# 3. 配置 API Key
cp deepseek-team/settings.example.json ~/.claude/settings.json
# 编辑 ~/.claude/settings.json，填入 DeepSeek 和多模态 API Key

# 4. 加载调度官行为
# 将 CLAUDE.md.example 的内容合并到你项目的 CLAUDE.md
```

### 启动

```bash
cgg    # 注意：是 cgg，不是 claude
```

首次启动后，调度官会自动询问你的多模态 API 配置（Key、Base URL、模型名）。提供后自动写入，后续无需重复。

---

## 怎么用

直接跟调度官说中文：

```
帮我看看这张截图 D:\bug.png
```

调度官判断：涉及图片 → 派视觉审查官（走千问多模态）→ 输出中文视觉报告 → 交付你。

```
帮我在登录页加一个"记住密码"的复选框
```

调度官判断：需求清晰 → 直接派码农（flash手 + pro大脑）→ 编码 → 自测 → 派审核官审查 → 终审 → 交付你。

```
这个登录接口偶发 500 错误，帮我排查
```

调度官判断：复杂 bug → 派调试工程师 → 读 diff/日志 → 定位根因 → 最小修复方案。

如果需求模糊（比如你说"优化一下性能"），它会先把项目负责人叫出来，跟你确认细节，产出规格书，再派码农。

**你不操心"该用哪个 agent"、"怎么跟 agent 说话"——调度官替你操心。**

---

## 调度官行为规则（v2 新增）

除了委派铁律，v2 新增了三套行为规则：

**消息分流判断**：用户消息先分类——闲聊/知识问答/进度查询/追问 → 直接对话模式；代码任务/图片分析/多角色协作 → 全团队模式。边界模糊时向用户确认。

**调度官禁区**：视觉审查官失败时，调度官不得自行用 Read 工具看图（DeepSeek 是纯文本模型，越权看图结果不可靠）。失败报错，不自替。

**派发后守则**：派完任务立刻回用户对话 + 列出运行中代理看板 + 继续对接。子代理干活 ≠ 调度官下班。

---

## 设计上的一些选择

**为什么纯中文？**
不是文化自信，是技术账。DeepSeek 原生的思维链就是中文的——这意味着它用中文思考不需要内部翻译，直接省掉了一层 token 开销。同样的任务，中文提示词比英文提示词少消耗 20-30% 的 token，因为模型不需要在脑子里先把英文转成中文再推理。

另一个原因：混入英文（比如 Bash 描述里写 "Run tests" 而不是 "运行测试"）会触发语言切换，打断思维链的连贯性。同样的提示词，纯中文锚定后产出质量差一个等级。说白了——让中文模型用中文干活，天经地义。

**为什么选 CGG 做多供应商调度？**
Claude Code 原生只支持单一模型。CGG 是源码级扩展，一份 settings.json 配多个供应商，子代理各自走不同模型。主会话 DeepSeek → 推理，视觉审查官走千问/GPT/Claude → 看图。一个终端、一套配置、一个会话——不需要外部编排脚本。

**为什么 agent 定义全是 markdown？**
因为 Claude Code 原生支持。不需要写代码定义 agent 行为，写 markdown 就行。改一个 agent 的行为就是改一个文件，改完立刻生效，不用重新部署。

**扩展为什么容易？**
框架已经搭好了，加新角色就是加一个 md 文件的事。比如你想加个"测试工程师"——把 `coder-agent.md` 复制一份，改 frontmatter 的 name/description，工具权限砍掉 Write/Edit 只留 Bash，审查清单换成测试覆盖率标准。五分钟上线。

更关键的是，框架不限制模型种类。DeepSeek 是纯文本模型，但视觉审查官走多模态模型，未来还可以加更多专用模型——各干各的长处，不用一棵树上吊死。

---

## 跟其他方案的区别

| | deepseek-team v2 | oh-my-claude | my-claude-devteam |
|---|---|---|---|
| 语言 | 纯中文 | 中英双语 | 英文（有繁中版） |
| 模型调度 | CGG 多供应商 | 11 个 provider | 任意 |
| Agent 数 | 5 | 11+6 | 12 |
| 安装 | git clone + cp 文件 | npm install | 插件市场 |
| 多模态视觉 | ✅ 独立多模态模型 | ✅ | ❌ |
| 分体式编码 | ✅ flash手+pro脑 | ❌ | ❌ |
| 消息分流 | ✅ 智能判断 | ❌ | ❌ |
| 学习成本 | 读完 CLAUDE.md 就行 | 需要读文档 | 需要读文档 |
| 适合谁 | 个人开发者、小团队 | 有运维能力的团队 | 追求完整流程的团队 |

说到底，deepseek-team 是一个**个人开发者自用的配置方案**，不是产品、不是框架、不是平台。它的价值在于你可以读懂、改得动、不被绑定。

---

## 文件说明

```
deepseek-team/                  # 仓库根目录
├── README.md                   # 项目说明 + 架构图
├── CHANGELOG.md                # 更新日志
├── settings.example.json       # API Key 配置模板（含占位符）
├── CLAUDE.md.example           # 调度官行为配置
└── agents/                     # 5 个 Agent 定义
    ├── boss-agent.md                 # 调度官
    ├── project-lead-agent.md         # 项目负责人
    ├── coder-agent.md                # 码农（分体式）
    ├── code-reviewer-agent.md        # 审核官
    ├── debugger-agent.md             # 调试工程师
    └── visual-inspector.md           # 视觉审查官（多模态）

~/.claude/                      # 用户本地部署后
├── settings.json               # 你的 API Key（已 gitignore）
└── agents/                     # 5 个 agent 文件
```

核心代码约 500 行 markdown。读完一遍不超过 15 分钟。

---

## 已知局限

- **必须 CGG**。v2 依赖 CGG 的多供应商调度能力，原生 Claude Code 无法使用视觉审查官。
- **多模态需要额外 API Key**。DeepSeek 是纯文本模型，视觉审查官需要你提供一个多模态供应商的 Key（千问/GPT/Claude/Gemini）。
- **默认 5 个 agent**。日常够用，但如果你要同时做前后端 + 数据库 + 部署 + 测试 + 安全 + ……需要自己加角色。好在一个新角色就是复制粘贴改几行的事。
- **没有内置记忆系统**。跨会话的上下文依赖 Claude Code 自己的记忆机制，没有 FTS5 语义搜索。
- **调度是规则驱动的**。调度官靠 CLAUDE.md 里的规则做判断，不是规则引擎，边界模糊时可能有决策抖动。

---

## 之后想做的

- [ ] 一个 `install.sh` / `install.ps1` 脚本，一条命令完成全部部署
- [ ] 项目负责人的规格书模板继续精简
- [ ] 加一个"测试工程师"角色，专门跑测试套件 + 覆盖率检查
- [ ] 考虑加本地记忆系统（FTS5 语义搜索），跨会话保留项目上下文

---

## 关于作者

在校大学生课余维护，更新随学期节奏波动。欢迎提 Issue 和意见。

---

## 许可

禁止商用。个人学习、研究、非商业用途自由使用。转载请注明出处。

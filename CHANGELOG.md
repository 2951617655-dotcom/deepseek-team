# 更新日志

## 2026-05-10

### 并行工作流（架构升级）

- **P0-P4 五阶段完整流水线**：委派铁律从线性流水线扩展为"快速通道 + 完整流水线"双模式。P0 需求细化 → P1 编码实现 → P2 并行质检 → P3 综合报告 → P4 终审交付
- **P2 并行质检**：审核官、调试工程师、视觉审查官可同时启动，任一失败不阻塞其他角色。调度官收集全部报告后由项目负责人 P3 综合
- **树形看板**：并行组在代理看板中用 `├` `└` 缩进展示，运行状态一目了然
- 兼容现有快速通道语法，默认串行，不破坏现有工作流

### 自定义角色体系（扩展性升级）

- **角色注册机制**：开发者可通过 `~/.claude/agents/custom/` 目录注册自定义角色（产品经理、安全审计员等），复用现有 frontmatter 格式
- **创建前置流程（硬性要求）**：审题 → 查 skill 库存 → 三选项询问用户（A 凭空创建 / B 基于库存优化 / C 去 GitHub 搜社区方案），杜绝从零凭空造轮子
- 注册表 + 交叉校验 + 工具权限预检，自定义角色无缝集成到现有工作流

### 结构化任务单与澄清协议（通信质量升级）

- **任务单格式规范**（`task-schema.md`）：Markdown 主体（技术上下文/交付物/约束验收/澄清记录）+ JSON 信封（5 字段极简），专为 LLM 阅读理解设计
- **澄清消息循环**：子代理发现信息不足时先自行 Read/Grep 查代码库，仍不足则发起澄清请求（≤2 轮）。调度官路由判断——代码库可查 → 回调项目负责人；用户意图 → 直接问用户
- **反馈记录机制**：同模式缺陷被审核官发现 ≥3 次 → 调度官记入 PROJECT_MEMORY.md 常驻约束区，未来派发自动提取
- 所有 agent 新增「第零步：解析任务单」，有则解析执行，无则降级纯文本，向后兼容

### 核心铁律（规则强化）

- **CLAUDE.md 顶部插入核心铁律区块**（3 条）：调用 Agent 后必须输出看板、面向用户输出必须中文、子代理失败上报不自替。解决关键指令被埋在文件深处导致模型注意力衰减的问题
- 所有 sub-agent 的「语言锚定」从文件末尾搬到 frontmatter 后第一段，强化措辞"输出中文三段之前禁止调用任何工具"

### 质检角色边界明确

- **审核官**：并行质检时不越界检查安全漏洞和 UI 问题，偶有跨域发现标注"[跨域发现]"
- **调试工程师**：并行质检时支持简化指令格式执行安全/性能扫描（跳过复现和二分定位）；bug 排查仍走完整六步流程
- **视觉审查官**：协同模式更新为 P3 综合环节合并，并行质检时与其他角色同时启动互不依赖

### 项目负责人新增能力

- **规格书模板扩展**：新增「并行策略」字段（串行/阶段并行）、「负责角色」列（支持自定义角色）、并行阶段表
- **P3 综合报告**：对比各方报告输出一致发现（高置信度）、互补发现、需人工裁决三类
- **澄清应答（第五步）**：被调度官回调时，只查代码库事实回答问题，不猜用户意图

### 新增文件

- `agents/task-schema.md`：结构化任务单完整格式规范（146 行）

### 改动的文件

- `CLAUDE.md.example`：新增核心铁律、P0-P4 完整流水线、并行工作流章节、自定义角色体系、结构化任务单与澄清协议
- `agents/project-lead-agent.md`：规格书模板扩展 + P3 综合 + 澄清应答
- `agents/coder-agent.md`：第零步任务单解析 + 澄清请求协议 + 完成报告块
- `agents/code-reviewer-agent.md`：并行质检职责边界 + 第零步任务单解析
- `agents/debugger-agent.md`：并行非调试任务边界 + 第零步任务单解析 + 调试专用澄清
- `agents/visual-inspector.md`：协同模式更新 + 第零步任务单解析

## 2026-05-08

### 架构升级

- **Claude Code CGG 多模型协作**：从单一 DeepSeek 升级为多供应商架构。主会话走 DeepSeek，视觉审查官独立走多模态模型（千问/GPT/Claude/Gemini 可选）
- **五层团队**：新增调试工程师（v4-pro，复杂 bug 定位 + 最小修复）和视觉审查官（多模态，截图/UI/PDF 分析）

### 调度官行为规则新增

- **消息分流判断**：用户消息先分类再行动——闲聊/知识问答直接回复，代码任务/图片分析启动团队
- **调度官禁区**：视觉审查官失败时不得自行看图（越权），失败报告用户等指令
- **派发后守则**：派完任务立刻回用户对话 + 列出运行中代理看板 + 继续对接

### 码农（coder-agent.md）

- **分体式码农**：核心升级——flash 做双手，每次编码前 spawn 一个 pro 子代理（大脑）输出 Edit 指令清单，双手只负责执行
- 新增 Agent 工具权限（用于 spawn 大脑子代理）
- Edit 匹配失败最多重试 2 次，全败上报调度官
- 报告格式改为「大脑给哪些指令 + 执行情况」

### 项目负责人（project-lead-agent.md）

- 规格书模板全面升级：背景与目标 → 影响范围 → 技术方案 → 任务拆解 → 逐任务详述（含输入输出/边界/不可做）→ 验收标准 → 风险提示
- 文件路径必须来自实际 Grep/Glob，不凭空想象
- 每次输出附摘要：共 N 个任务，预估总行数 M，涉及 K 个文件

### 审核官（code-reviewer-agent.md）

- git diff 策略优化：先收集未 staged 变更，再收集 staged，合并两份集合避免遗漏
- 语言锚定升级为三段式：审什么 / 关注什么风险 / 不确定什么

### 新增文件

- `settings.example.json`：API Key 配置模板，含全部占位符
- `agents/debugger-agent.md`：调试工程师 agent 定义
- `agents/visual-inspector.md`：视觉审查官 agent 定义（provider: openai, model: qwen3-vl-flash）

### 新用户引导

- CLAUDE.md 新增「多模态供应商配置」章节：调度官主动询问用户 API Key、Base URL、模型名，自动写入 settings.json

---

## 2026-05-10（后续更新）

### 测试工程师（v4-pro）

- 新增 `agents/custom/tester.md`：v4-pro 模型，TDD/事后补测双模式
- 覆盖单元/集成/E2E/性能测试，含 Mock 策略规范、覆盖率目标、测试质量评估
- 集成到 P1（测试设计）和 P2（测试验证），与审核官/调试工程师互补

### 全员补齐（社区成功案例驱动）

- 7 角色 40 能力缺口审计，基于 Anthropic C 编译器实验、specwork、AutoGen 等项目补齐
- 码农大脑工具升级（Read→Read/Glob/Grep）、审核官自动化检查(linter)、调试工程师安全扫描+性能分析、视觉审查官截图对比模式
- 项目负责人规格书新增方案对比表+API变更+数据库变更、P3综合定量汇总
- 调度官 DAG 编排（depends_on）、PROJECT_MEMORY 用户偏好区

### 配置文件整理（12 问题审计）

- 审计 8 个配置文件，修复 4 事实错误 + 2 规则冲突 + 2 位置不佳 + 4 冗余噪音
- 语言锚定从 312 行移到核心铁律之后；visual-inspector 锚定置顶
- 删除 ~57 处 HTML 注释；历史记录移入独立文件；决策案例库空表删除
- CLAUDE.md 从 534 行压缩到 ~460 行（-13.5%）
- 核心铁律 #1 加时间约束："同一轮回复中"；派发后看板升级为"必须格式"

### 码农大脑强制化 + AI 可读性优化

- coder.md 铁律新增 #0：编码前必须经 pro 大脑分析
- 全项目叙事体→指令体压缩、Emoji → 中文标签（[致命][重要][建议]）、HTML 注释清理
- 8 个文件优化，节省约 8-12% token

### 大脑架构上移（解决嵌套限制）

- 旧：`调度官→派码农→码农 spawn 大脑(不稳定)`
- 新：`调度官→spawn 大脑(顶层,100%可用)→大脑出指令→派码农执行`
- coder.md tools 去掉 Agent，码农从"自己分析"变为"接收大脑指令逐条执行"
- 纯机械替换任务豁免大脑步骤

### AskUserQuestion 预汉化（小白友好）

- **预汉化规则**：调度官调用 `AskUserQuestion` 前必须先输出中文说明，包含询问目的、选项通俗解释、英文界面预警三要素
- **双受众区分**：规则层给 AI 读（精确结构化、含格式约束和违规判定），模板层给用户看（通俗中文、零门槛、固定标题 `### 我需要你帮我做个选择`）
- **违规接入核心铁律**：未输出预汉化直接调用 AskUserQuestion → 违反核心铁律第2条
- 涉及文件：`CLAUDE.md`、`~/.claude/CLAUDE.md`、`CLAUDE.md.example` 三文件同步追加
